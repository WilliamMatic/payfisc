DELETE s FROM series s JOIN (     SELECT nom_serie, MIN(id) AS vrai_id     FROM series     GROUP BY nom_serie     HAVING COUNT(*) > 1 ) t ON s.nom_serie = t.nom_serie LEFT JOIN engins e ON e.serie_id = s.id WHERE s.id > t.vrai_id AND e.serie_id IS NULL;